specVersion: "3.0"
metadata:
  name: vendormaster
type: application
resources:
  configuration:
    paths:
      webapp: webapp
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      configuration:
         baseUri: "http://localhost:4004"
         mountPath: "/vendor"